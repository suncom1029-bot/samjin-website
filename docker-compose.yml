version: '3.8'

services:
  web:
    build: .
    container_name: samjin_dev
    ports:
      - "8080:80"
    volumes:
      - ./:/var/www/html
    environment:
      - PORT=80
    working_dir: /var/www/html
